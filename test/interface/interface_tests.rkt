#lang racket

(require "../../app/interface.rkt")
(provide run_interface_test)

(define (run_interface_test)
  (display "INTERFACE TESTS-------------------------------- \n")
  (cond ((and)))
          ;(run_neighbours_map)
  (display "INTERFACE TESTS-------------------------------- \n"))